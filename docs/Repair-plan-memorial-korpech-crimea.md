Да. Я подготовила **единый окончательный регламент**, уже не просто как перечень замечаний, а именно как рабочий документ для ИИ-агента, который будет править код.

Я опиралась на сам текст Revision-01 из загруженного отчёта: в нём зафиксированы 2 замечания высокой важности, 4 средней и 3 низкой; отдельно учтены `site.url`, `Head.astro`, sitemap, RSS/Atom, Partytown/GA/Umami, OG, локали, Waline, `apply-lqip`, `public/fonts`.

**Важное исправление относительно моего предыдущего варианта:** я включила в регламент отдельный **SEO-аудит и коррекцию кода поисковой индексации**, но не превращаю его в произвольное «улучшение SEO». Агент должен работать только с тем, что подтверждено кодом и Revision-01.

# РЕГЛАМЕНТ ИСПРАВЛЕНИЯ REVISION-01

**Проект:** Memorial Korpech Crimea
**Стек:** Astro 6 / SSG / multilingual / EdgeOne Pages
**Основание:** Revision-01 — аудит репозитория
**Назначение документа:** последовательное устранение замечаний ревизии с помощью ИИ-агента, компетентного в правке кода.

---

## 1. ГЛАВНЫЙ ПРИНЦИП

### ОДИН ПРОМТ → ОДНА ОПЕРАЦИЯ → ОДНА ПРОВЕРКА → СТОП

ИИ-агент НЕ должен получать весь список исправлений одним заданием.

Каждая операция выполняется отдельно.

После каждой операции агент обязан:

1. выполнить только указанное изменение;
2. проверить результат;
3. сообщить:

   * какие файлы изменены;
   * что именно изменено;
   * результат проверки;
   * возникшие вопросы или неопределённости;
4. **остановиться**.

Следующая операция начинается только отдельным промтом.

---

## 2. ЖЁСТКИЕ ОГРАНИЧЕНИЯ ДЛЯ АГЕНТА

Во всех операциях действуют следующие правила.

### Запрещено

* делать рефакторинг, не указанный в операции;
* менять архитектуру проекта;
* менять дизайн;
* менять тексты и контент без отдельного задания;
* удалять зависимости только потому, что они «кажутся ненужными»;
* удалять файлы без прямого указания;
* менять структуру проекта шире, чем требуется конкретной операцией;
* самостоятельно выбирать production-домен;
* самостоятельно принимать решение о наборе языков;
* самостоятельно включать Google Analytics или Umami;
* самостоятельно добавлять новые SEO-механизмы;
* самостоятельно менять стратегию генерации OG;
* объединять несколько операций;
* исправлять другие найденные проблемы «заодно».

### Обязательно

Если для выполнения операции требуется решение владельца проекта, агент должен **остановиться и задать вопрос**.

Если проверка показывает, что проблема отсутствует, агент ничего не меняет и фиксирует:

> «Замечание проверено. Изменение не требуется».

---

## 3. ПОРЯДОК ВЫПОЛНЕНИЯ

| №  | Операция                                   | Приоритет | Тип                         |
| -- | ------------------------------------------ | --------- | --------------------------- |
| 0  | Зафиксировать исходное состояние           | 🔴        | диагностика                 |
| 1  | Определить production-домен для `site.url` | 🔴        | требуется решение владельца |
| 2  | Исправить `site.url`                       | 🔴        | код                         |
| 3  | Проверить SEO-код `Head.astro`             | 🔴        | аудит                       |
| 4  | Проверить SEO URL после изменения домена   | 🔴        | аудит                       |
| 5  | Проверить sitemap                          | 🔴        | аудит                       |
| 6  | Проверить `robots.txt`                     | 🔴        | аудит                       |
| 7  | Проверить RSS/Atom и абсолютные ссылки     | 🔴        | аудит                       |
| 8  | Проверить SEO локалей                      | 🟠        | аудит                       |
| 9  | Проверить и скорректировать аналитику      | 🟠        | код                         |
| 10 | Сделать Partytown условным                 | 🟠        | код                         |
| 11 | Проверить генерацию OG для draft           | 🟠        | аудит/код                   |
| 12 | Исправить генерацию OG для draft           | 🟠        | код, если подтверждено      |
| 13 | Убрать build-файлы из `public`             | 🟠        | код/структура               |
| 14 | Принять решение по локалям                 | 🟠        | решение владельца           |
| 15 | Реализовать выбранную стратегию локалей    | 🟠        | код                         |
| 16 | Исправить `jp-JP` → `ja-JP`                | 🟢        | код                         |
| 17 | Проверить `apply-lqip`                     | 🟢        | аудит                       |
| 18 | Проверить build chain                      | 🟢        | аудит                       |
| 19 | Проверить объём `public/fonts`             | 🟢        | аудит                       |
| 20 | Финальная проверка после всех исправлений  | 🔴        | контроль                    |

---

## 4. ОПЕРАЦИЯ 0 — ИСХОДНАЯ ТОЧКА

### Цель

Зафиксировать состояние проекта до начала исправлений.

### ПРОМТ АГЕНТУ

> Проведи только диагностическую операцию.
>
> Не изменяй ни одного файла.
>
> Зафиксируй текущее состояние проекта перед устранением замечаний Revision-01:
>
> 1. текущий git status;
> 2. текущую ветку;
> 3. наличие незакоммиченных изменений;
> 4. текущий результат `astro check`;
> 5. текущий результат `astro build`, если выполнение возможно.
>
> Ничего не исправляй.
>
> В конце покажи результат диагностики и остановись.

### Результат агента

☐ Исходное состояние зафиксировано
☐ Изменений в коде нет
☐ Агент остановился

**Результат выполнения:**
`________________________________________________`

---

## 5. ОПЕРАЦИЯ 1 — PRODUCTION-ДОМЕН

### Основание Revision-01

В `src/config.ts:17`:

`themeConfig.site.url`

жёстко задан как:

`https://astro-blog.edgeone.dev`

При SSG этот адрес используется для canonical, Open Graph, sitemap, RSS/Atom и абсолютных ссылок. Поэтому неправильный домен приведёт к генерации неправильных SEO URL.

### ВАЖНО

**Агент НЕ имеет права угадывать production-домен.**

### ПРОМТ АГЕНТУ

> Проверь `src/config.ts` и найди источник значения `themeConfig.site.url`.
>
> Ничего не изменяй.
>
> Подтверди:
>
> * текущее значение;
> * где оно используется;
> * какие файлы зависят от него.
>
> Если для исправления требуется определить фактический production-домен, задай мне этот вопрос.
>
> Ничего больше не изменяй.
>
> После отчёта остановись.

☐ Проверено
☐ Домен подтверждён владельцем
☐ Агент остановился

**Production-домен:**
`________________________________________________`

---

## 6. ОПЕРАЦИЯ 2 — ИСПРАВЛЕНИЕ `site.url`

### ПРОМТ АГЕНТУ

> На основании подтверждённого production-домена измени только источник `themeConfig.site.url` в `src/config.ts`.
>
> Не меняй другие SEO-механизмы.
>
> Не делай рефакторинг.
>
> После изменения:
>
> 1. проверь TypeScript;
> 2. проверь, что значение действительно используется проектом;
> 3. покажи diff.
>
> Не исправляй другие найденные проблемы.
>
> После проверки остановись.

☐ `site.url` исправлен
☐ Diff проверен
☐ Других изменений нет
☐ Агент остановился

**Результат:**
`________________________________________________`

---

## 7. ОПЕРАЦИЯ 3 — SEO-КОД `Head.astro`

### Цель

Не «улучшить SEO», а проверить конкретные SEO-элементы, которые зависят от `site.url`.

### Проверить

`src/layouts/Head.astro`

В частности:

* `canonical`;
* `og:url`;
* `og:image`;
* связанные абсолютные URL;
* title;
* description;
* robots;
* Open Graph;
* Twitter Card, если уже присутствует;
* locale/lang, если уже присутствуют.

### ПРОМТ АГЕНТУ

> Проведи только SEO-аудит `src/layouts/Head.astro`.
>
> Ничего не изменяй.
>
> Проверь существующий код и зафиксируй:
>
> 1. как формируется canonical;
> 2. как формируется `og:url`;
> 3. как формируется `og:image`;
> 4. откуда берутся title и description;
> 5. есть ли robots/noindex;
> 6. какие SEO URL используют `themeConfig.site.url`;
> 7. есть ли очевидные ошибки именно в существующей реализации.
>
> Не добавляй новые SEO-теги и не переписывай существующую систему.
>
> Если проблема подтверждена — перечисли конкретные строки и предложи минимальное исправление.
>
> Файлы не изменяй.
>
> После отчёта остановись.

☐ SEO Head проверен
☐ Изменений нет
☐ Проблемы перечислены
☐ Агент остановился

**Результат:**
`________________________________________________`

---

## 8. ОПЕРАЦИЯ 4 — КОРРЕКЦИЯ SEO В `Head.astro`

Выполняется **только если операция 3 выявила реальную ошибку**, не устранённую изменением `site.url`.

### ПРОМТ АГЕНТУ

> Исправь только ту SEO-проблему в `src/layouts/Head.astro`, которую мы подтвердили в предыдущей операции.
>
> Выполни минимальное изменение.
>
> Не добавляй новые SEO-функции.
>
> Не меняй дизайн, контент, маршрутизацию или структуру компонентов.
>
> После изменения выполни доступную проверку и покажи diff.
>
> Остановись.

☐ Исправление выполнено
☐ Diff проверен
☐ Проверка пройдена
☐ Агент остановился

---

## 9. ОПЕРАЦИЯ 5 — SITEMAP

### Файл из Revision-01

`src/pages/robots.txt.ts:5` связан с генерацией sitemap URL.

### ПРОМТ АГЕНТУ

> Проведи только аудит sitemap и связанных с ним URL.
>
> Найди фактическую реализацию sitemap в проекте.
>
> Ничего не изменяй.
>
> Проверь:
>
> * используется ли правильный `site.url`;
> * нет ли старого `astro-blog.edgeone.dev`;
> * не формируются ли неправильные абсолютные URL;
> * присутствуют ли маршруты, которые не должны попадать в sitemap;
> * связана ли проблема с локалями.
>
> Отдельно укажи подтверждённые ошибки и только затем возможные исправления.
>
> После отчёта остановись.

☐ Sitemap проверен
☐ Старый домен проверен
☐ Лишние URL проверены
☐ Агент остановился

---

## 10. ОПЕРАЦИЯ 6 — `robots.txt`

### ПРОМТ АГЕНТУ

> Проведи только аудит `robots.txt`.
>
> Проверь существующий `src/pages/robots.txt.ts`.
>
> Ничего не изменяй.
>
> Проверь:
>
> * правила индексации;
> * отсутствие случайного `Disallow`, блокирующего сайт;
> * корректность ссылки на sitemap;
> * корректность production-домена;
> * соответствие фактической SSG-структуре проекта.
>
> Не добавляй новые правила без подтверждённой необходимости.
>
> После отчёта остановись.

☐ robots.txt проверен
☐ Sitemap URL проверен
☐ Агент остановился

---

## 11. ОПЕРАЦИЯ 7 — RSS / ATOM / АБСОЛЮТНЫЕ ССЫЛКИ

### Файл

`src/utils/feed.ts`

Revision-01 указывает на строки `114`, `128-130`, `159`.

### ПРОМТ АГЕНТУ

> Проведи только аудит `src/utils/feed.ts`.
>
> Ничего не изменяй.
>
> Проверь:
>
> * абсолютные URL;
> * URL статей;
> * URL feed;
> * использование `themeConfig.site.url`;
> * наличие старого домена;
> * корректность ссылок после изменения production-домена.
>
> Если обнаружена ошибка — укажи точное место и минимальное исправление.
>
> После отчёта остановись.

☐ Feed проверен
☐ Абсолютные URL проверены
☐ Старый домен проверен
☐ Агент остановился

---

## 12. ОПЕРАЦИЯ 8 — SEO МНОГОЯЗЫЧНЫХ МАРШРУТОВ

Revision-01 установил, что контент сейчас существует только для `ru`, однако технически генерируются маршруты `en/es/ja/zh/zh-tw`, включая homepage/about/feed/sitemap. Это создаёт риск пустых индексируемых страниц.

### ПРОМТ АГЕНТУ

> Проведи только аудит SEO локализованных маршрутов.
>
> Ничего не изменяй.
>
> Проверь:
>
> * `lang`;
> * canonical;
> * локализованные URL;
> * sitemap;
> * feed;
> * наличие пустых страниц;
> * существующую реализацию multilingual SEO.
>
> Не меняй список языков.
>
> В конце раздели результат на:
>
> 1. подтверждённые проблемы;
> 2. последствия;
> 3. что требуется решить владельцу проекта.
>
> После отчёта остановись.

☐ Локали проверены
☐ SEO локалей проверено
☐ Решение владельца не подменено агентом

---

## 13. ОПЕРАЦИЯ 9 — АНАЛИТИКА

Revision-01 отмечает, что GA/Umami ID сейчас пусты, поэтому соответствующий код в `Head.astro` не выполняется, но сама интеграция Partytown всё равно подключена.

### ПРОМТ АГЕНТУ

> Проведи аудит существующей аналитики.
>
> Проверь только:
>
> * `src/config.ts`;
> * `src/layouts/Head.astro`;
> * места, где используются GA/Umami;
> * условия загрузки аналитики;
> * поведение при пустом ID.
>
> Ничего не изменяй.
>
> Ответь:
>
> 1. какие системы аналитики предусмотрены;
> 2. включены ли они сейчас;
> 3. загружается ли аналитический код при пустых ID;
> 4. есть ли лишние запросы;
> 5. какие минимальные изменения требуются.
>
> Не подключай аналитику самостоятельно.
>
> После отчёта остановись.

☐ Аналитика проверена
☐ GA проверен
☐ Umami проверен
☐ Пустые ID проверены
☐ Агент остановился

---

## 14. ОПЕРАЦИЯ 10 — PARTYTOWN

### Основание

`@astrojs/partytown` подключается в `astro.config.ts:49-53`, несмотря на пустые GA/Umami ID. Revision-01 рекомендует условное подключение Partytown только при наличии статистики.

### ПРОМТ АГЕНТУ

> Измени только подключение `@astrojs/partytown`.
>
> Требование:
>
> * когда аналитика не настроена — Partytown не должен создавать лишний loader/worker;
> * когда в будущем будет настроен поддерживаемый analytics ID — существующая возможность аналитики должна сохраниться.
>
> Не удаляй поддержку GA/Umami.
>
> Не меняй саму аналитику.
>
> Не меняй `Head.astro`, если это не является технически необходимым именно для этой операции.
>
> После изменения:
>
> 1. выполни проверку;
> 2. покажи diff;
> 3. подтверди поведение при пустых ID.
>
> Остановись.

☐ Partytown условный
☐ Аналитика не потеряна
☐ Проверка выполнена
☐ Агент остановился

---

## 15. ОПЕРАЦИЯ 11 — OG IMAGE: ПРОВЕРКА DRAFT

### Файл

`src/pages/og/[...image].ts`

Revision-01 указывает, что PNG создаётся для каждой статьи, включая draft. Это может увеличивать время и память сборки.

### ПРОМТ АГЕНТУ

> Проведи только аудит `src/pages/og/[...image].ts`.
>
> Ничего не изменяй.
>
> Проверь:
>
> * как определяется список статей;
> * учитывается ли `draft`;
> * для каких материалов генерируется OG PNG;
> * можно ли исключить draft минимальным изменением;
> * какие существующие страницы зависят от этих изображений.
>
> Не меняй код.
>
> После отчёта остановись.

☐ OG генерация проверена
☐ Draft проверен
☐ Зависимости проверены
☐ Агент остановился

---

## 16. ОПЕРАЦИЯ 12 — ИСКЛЮЧЕНИЕ DRAFT ИЗ OG

Выполняется только после подтверждения операции 11.

### ПРОМТ АГЕНТУ

> Исправь только генерацию OG в `src/pages/og/[...image].ts`.
>
> Требование:
>
> * draft-контент не должен порождать ненужные OG PNG;
> * опубликованные материалы должны продолжить получать OG images;
> * существующий механизм генерации не переписывать.
>
> Сделай минимальное изменение.
>
> После изменения выполни проверку build/генерации маршрутов, если она доступна.
>
> Покажи diff и остановись.

☐ Draft исключён
☐ Published OG сохранён
☐ Проверка пройдена
☐ Агент остановился

---

## 17. ОПЕРАЦИЯ 13 — BUILD-ФАЙЛЫ В `public`

### Основание

В:

`public/fonts/Font Subset List/`

находятся `.txt` и `unicode_range.py`, предназначенные для создания font subsets.

Поскольку `public` публикуется как статический ресурс, эти файлы могут попадать на CDN. Revision-01 рекомендует перенести их из `public`.

### ПРОМТ АГЕНТУ

> Проведи только аудит `public/fonts/Font Subset List/`.
>
> Ничего не удаляй и не перемещай.
>
> Определи:
>
> 1. какие файлы являются build-инструментами;
> 2. какие файлы являются реально используемыми web-ресурсами;
> 3. есть ли ссылки на эти файлы из кода;
> 4. какие из них действительно публикуются в `dist`.
>
> После анализа предложи минимальный безопасный перенос build-инструментов из `public`.
>
> Файлы пока не изменяй.
>
> Остановись.

☐ Файлы классифицированы
☐ Использование проверено
☐ Агент остановился

---

## 18. ОПЕРАЦИЯ 14 — ПЕРЕМЕЩЕНИЕ BUILD-ФАЙЛОВ

### ПРОМТ АГЕНТУ

> Перемести только подтверждённые build-инструменты из `public/fonts/Font Subset List/` в непубличное место репозитория.
>
> Требования:
>
> * не удалять используемые web-font файлы;
> * не менять CSS;
> * не менять URL шрифтов;
> * не менять внешний вид;
> * не менять механизм загрузки шрифтов;
> * сохранить возможность использовать build-инструменты.
>
> После переноса проверь отсутствие этих инструментов в `dist`.
>
> Покажи список перемещённых файлов и diff.
>
> Остановись.

☐ Build-файлы перемещены
☐ Шрифты сохранены
☐ CSS не изменён
☐ Build проверен
☐ Агент остановился

---

## 19. ОПЕРАЦИЯ 15 — РЕШЕНИЕ ПО ЛОКАЛЯМ

Это **не техническое решение агента**.

Revision-01 обнаружил, что содержимое есть только для русского языка, а пять дополнительных языков технически генерируются.

Необходимо выбрать один вариант:

### Вариант A

Сейчас проект только русский.

Тогда:

`moreLocales`

сужается до фактически используемых языков.

### Вариант B

Многоязычный проект должен работать уже сейчас.

Тогда для каждой локали необходимо подготовить соответствующий контент.

### ПРОМТ АГЕНТУ

> Ничего не меняй.
>
> Покажи текущую конфигурацию `moreLocales` и фактически существующий контент по языкам.
>
> Подготовь два технических варианта:
>
> A — оставить только фактически наполненные локали;
>
> B — сохранить все заявленные локали и добавить недостающий контент.
>
> Не выбирай вариант самостоятельно.
>
> После отчёта остановись.

**Выбор владельца:**

☐ A — только фактически наполненные локали
☐ B — полноценная многоязычность
☐ Другое: `____________________________`

---

## 20. ОПЕРАЦИЯ 16 — РЕАЛИЗАЦИЯ ВЫБРАННОЙ СТРАТЕГИИ ЛОКАЛЕЙ

После выбора владельца.

### Если выбран A

> Измени только `moreLocales` в `src/config.ts` в соответствии с утверждённым списком локалей.
>
> Не меняй другие multilingual-механизмы.
>
> Проверь, что пустые языковые страницы больше не генерируются.
>
> Проверь sitemap.
>
> Покажи diff и остановись.

### Если выбран B

Работа выполняется **отдельными операциями для каждого языка**.

Не разрешается одним промтом создавать весь переводческий контент.

---

## 21. ОПЕРАЦИЯ 17 — WALINE

### Основание

В:

`src/i18n/config.ts:58`

обнаружено:

`'ja': 'jp-JP'`

Revision-01 предполагает, что должно быть:

`'ja-JP'`.

При этом Waline сейчас отключён (`enabled: false`), поэтому замечание не влияет на текущую production-работу.

### ПРОМТ АГЕНТУ

> Исправь только ошибку локали Waline:
>
> `'ja': 'jp-JP'`
>
> на подтверждённое корректное значение:
>
> `'ja-JP'`.
>
> Не включай Waline.
>
> Не меняй другие локали.
>
> Выполни проверку и покажи diff.
>
> Остановись.

☐ Исправлено
☐ Waline не включён
☐ Проверка пройдена
☐ Агент остановился

---

## 22. ОПЕРАЦИЯ 18 — `apply-lqip`

### Основание

`package.json` запускает:

`astro check && astro build && pnpm apply-lqip`

а `scripts/apply-lqip.ts` записывает данные в:

`src/assets/lqip-map.json`.

Revision-01 отмечает зависимость post-build обработки от возможности записи в исходный каталог.

### ПРОМТ АГЕНТУ

> Проведи только аудит `scripts/apply-lqip.ts`.
>
> Ничего не изменяй.
>
> Проверь:
>
> * какие файлы он читает;
> * какие файлы создаёт или изменяет;
> * действительно ли требуется запись в `src/assets/lqip-map.json`;
> * что происходит, если каталог доступен только для чтения;
> * есть ли фактический риск для текущего EdgeOne build.
>
> Не меняй архитектуру.
>
> Не переноси файлы.
>
> Не меняй build command.
>
> После отчёта остановись.

☐ Зависимость проверена
☐ Риск подтверждён/опровергнут
☐ Агент остановился

---

## 23. ОПЕРАЦИЯ 19 — BUILD CHAIN

### ПРОМТ АГЕНТУ

> Проведи только аудит текущей команды сборки:
>
> `astro check && astro build && pnpm apply-lqip`
>
> Ничего не изменяй.
>
> Проверь:
>
> 1. что произойдёт при ошибке `astro check`;
> 2. что произойдёт при ошибке `astro build`;
> 3. что произойдёт при ошибке `apply-lqip`;
> 4. нужен ли `apply-lqip` текущему проекту;
> 5. есть ли подтверждённая причина менять цепочку.
>
> Не меняй `package.json`.
>
> После отчёта остановись.

☐ Build chain проверен
☐ Изменение не внесено без основания
☐ Агент остановился

---

## 24. ОПЕРАЦИЯ 20 — FONT INVENTORY

Revision-01 указывает примерно 82 файла в `public/fonts`, около 20 MB, включая 71 EarlySummer shard. При этом `unicode-range` ограничивает фактическую загрузку браузером, поэтому это прежде всего вопрос объёма deploy/build, а не автоматическое основание для удаления шрифтов.

### ПРОМТ АГЕНТУ

> Проведи только инвентаризацию `public/fonts`.
>
> Ничего не удаляй.
>
> Ничего не оптимизируй.
>
> Покажи:
>
> * количество файлов;
> * общий размер;
> * группы шрифтов;
> * наличие unicode-range;
> * какие шрифты реально подключаются CSS;
> * какие файлы выглядят неиспользуемыми.
>
> Не удаляй и не объединяй файлы.
>
> После отчёта остановись.

☐ Инвентаризация выполнена
☐ Удалений нет
☐ Агент остановился

---

## 25. ФИНАЛЬНАЯ SEO-ПРОВЕРКА

Эта операция выполняется **после исправлений**, а не вместо них.

### ПРОМТ АГЕНТУ

> Выполни только финальную проверку SEO после внесённых изменений.
>
> Сначала выполни production build.
>
> Затем проверь содержимое `dist`.
>
> Проверь:
>
> 1. отсутствие старого домена `astro-blog.edgeone.dev`;
> 2. canonical;
> 3. `og:url`;
> 4. `og:image`;
> 5. title;
> 6. description;
> 7. robots;
> 8. sitemap;
> 9. RSS/Atom;
> 10. абсолютные ссылки;
> 11. локализованные URL;
> 12. отсутствие случайного `noindex`;
> 13. отсутствие draft-страниц там, где они не должны публиковаться;
> 14. отсутствие build-инструментов в `dist`.
>
> Не исправляй найденные проблемы.
>
> Составь итоговую таблицу:
>
> `Проверка | Результат | Ошибка | Файл`
>
> После отчёта остановись.

---

## 26. ФИНАЛЬНАЯ ПРОВЕРКА ПРОЕКТА

### ПРОМТ АГЕНТУ

> Проведи только финальную техническую проверку проекта после завершения Revision-01.
>
> Выполни:
>
> * `astro check`;
> * production build;
> * проверку git diff;
> * проверку `dist`;
> * поиск старого production-домена;
> * проверку sitemap/robots/feed;
> * проверку SEO URL;
> * проверку аналитики;
> * проверку отсутствия лишнего Partytown;
> * проверку OG;
> * проверку локалей;
> * проверку того, что build-файлы не публикуются.
>
> Не вноси никаких изменений.
>
> Итог представь в формате:
>
> `Операция | Выполнено | Проверка | Примечание`
>
> В конце отдельно укажи:
>
> **«Revision-01 устранена полностью»**
>
> только если все обязательные операции действительно подтверждены.
>
> Если что-либо осталось — перечисли конкретные пункты.
>
> После отчёта остановись.

---

## 27. ФИНАЛЬНЫЙ ЧЕК-ЛИСТ

### Высокий приоритет

☐ Production-домен подтверждён
☐ `themeConfig.site.url` исправлен
☐ Старый домен отсутствует в production build
☐ Canonical проверен
☐ `og:url` проверен
☐ `og:image` проверен
☐ Sitemap проверен
☐ robots.txt проверен
☐ RSS/Atom проверен
☐ Абсолютные URL проверены

### Аналитика

☐ GA проверен
☐ Umami проверен
☐ Пустые analytics ID корректно обрабатываются
☐ Partytown не создаёт лишнюю нагрузку без аналитики
☐ Будущая возможность аналитики сохранена

### OG

☐ Генерация OG проверена
☐ Draft проверен
☐ Draft не создаёт ненужные OG images

### Публикация

☐ Build-инструменты убраны из `public`
☐ Web-fonts сохранены
☐ CSS не сломан
☐ Build-инструменты не попадают в `dist`

### Локали

☐ Принято решение по языкам
☐ `moreLocales` соответствует решению
☐ Пустые локализованные страницы устранены либо контент добавлен
☐ Sitemap соответствует фактическим локалям
☐ SEO локалей проверено

### Низкий приоритет

☐ Waline `ja-JP` исправлен
☐ Waline не включён самовольно
☐ `apply-lqip` проверен
☐ Build chain проверен
☐ Fonts инвентаризированы
☐ Ненужные шрифты не удалялись без отдельного решения

---

## 28. ПРАВИЛО ОТЧЁТА АГЕНТА

После **каждой** операции агент обязан использовать следующий формат:

### Выполненная о��ерация

`№ ______`

### Изменённые файлы

`____________________________`

### Что изменено

`____________________________`

### Проверка

`____________________________`

### Ошибки

`____________________________`

### Требуется решение владельца

`Да / Нет`

### Следующая операция

`Не выполнять. Ожидать отдельный промт.`

---

## 29. КРИТЕРИЙ ЗАВЕРШЕНИЯ

Работа считается завершённой только тогда, когда:

1. все обязательные операции выполнены;
2. все изменения соответствуют Revision-01;
3. production build проходит;
4. `astro check` проходит;
5. SEO URL используют правильный production-домен;
6. старый домен отсутствует в production output;
7. sitemap/robots/feed согласованы;
8. аналитика не создаёт лишнюю нагрузку при пустых ID;
9. Partytown не работает без необходимости;
10. draft не создаёт ненужные OG images;
11. build-инструменты не публикуются через `public`;
12. принято и реализовано решение по локалям;
13. Waline typo исправлен без включения Waline;
14. `apply-lqip` и build chain проверены;
15. никаких дополнительных рефакторингов агентом не выполнено.

---

## 30. ГЛАВНОЕ ПРАВИЛО ПРОЕКТА

**Revision-01 не является поводом переписывать проект.**

Аудит признал архитектуру проекта в целом разумной и пригодной для EdgeOne Pages.

Цель данного регламента:

> **не улучшать проект по усмотрению ИИ, а точно, проверяемо и минимально устранить замечания Revision-01.**

Любое изменение, которого нет в настоящем регламенте, должно быть вынесено в отдельное задание и не выполняться «заодно».

Это уже можно использовать как **основной регламент рядом с проектом**. В частности, я специально отделила **SEO-проверку от простого исправления `site.url`**: теперь агент должен пройти `Head.astro → sitemap → robots → RSS/Atom → локализованные SEO URL → финальную проверку `dist`**, а не считать изменение домена полной коррекцией SEO. Это прямо следует из того, что в Revision-01 один и тот же `site.url` влияет на несколько SEO- и feed-механизмов.

Если хотите сохранить документ как файл `.md` или `.docx`, я могу также оформить **этот же регламент без изменения содержания** в нужном формате.