# TASK-0005 rev.2 · мемориал · qwen3-memorial · перепрогон по уточнениям владельца
Статус: NEW (перепрогон; материалы rev.1 НЕ приняты, коммитов не было)
1. Slug однажды, латиницей: bitva-za-krym-1942-<транслит-ФИО>-<pmn|obd>-<document_id>.
   Маркеров смерти в slug и человеческих текстах нет. Slug неизменяем (кормит бота).
   1469 slug rev.1 переотлить.
2. Filename = slug, единое правило, латиница.
3. Очередь = уточнение ПРИ карточке: строка с ФИО и датой гибели = карточка
   (processed или needs_manual_review); запись clarify.queue.json ссылается
   на id карточки, не заменяет её. 3107 имён из очереди rev.1 вернуть в карточки.
4. Слияния: один портал (obd), ФИО совпало, индексы разные → STOP и список
   оператору, автослияние запрещено. Разные порталы, 3–4 позиции → одна
   карточка: индекс и фото = pmn; канон формулировок = ОБД (П1); второй в sources[].
5. Сдался в плен / плен: needs_manual_review + draft: true + код pow_check;
   публикация — только решением админа. (ждёт слова владельца)
6. Пересборка полей разрешена (захоронение, призыв, звание), кроме slug и filename.
7. Полнота сниппетов ОК: нет поля → код snippet_gap, карточка создаётся.
8. Сортировку ленты не менять до решения владельца; текущее правило — в NOTES.
9. Цепочка захоронения (В6): burial.history[] с kind: primary|exhumation|reburial;
   первичные точки вида «высота 71,3», «высота 66,3 (Окопная)», «домик, где умер
   от ран» — нормализовать через locations, дыры → уточнение при карточке.
10. memorial_id: ссылка на текущий мемориал справочника src/content/memorials/;
    нет ссылки → код memorial_unlinked, карточка создаётся. Батч любого
    мемориала обязан проходить (В5).
11. engraved: yes|no|unknown, null запрещён (В3); unknown → код stone_check.
12. Поиск-индекс: только last_name + first_name (В4); отчество вне индекса.
13. Витрины слоя 2 (В2) конвертер НЕ строит: гарантирует только поля для них.
    Витрины — отдельная задача TASK-0010 после закрытия rev.2.
14. conscription_location (R-13): raw (байт-в-байт CSV, П1) + norm (словарь
    locations) + modern (современное имя из того же словаря). Вид на карточке:
    «призыв: <norm> (совр. <modern>)»; modern не подменяет raw/norm нигде.
    Нет modern в словаре → код modern_gap, карточка создаётся.
15. Карточка (CardFull.astro): строка локации призыва по п.14; иные блоки
    компонента не трогать.
Белый список дополнить: src/components/CardFull.astro (только строка локации
призыва); data/dictionaries/locations.* (поле modern).
Контроль дополнить: подмен raw/norm на modern = 0 (grep по отчёту);
манифест + поле conscription_modern.
Контроль: astro check 0 errors; перезаписей 0; grep «camo|цамо» по slug/filename = 0;
engraved null = 0; манифест: id, slug, ФИО кириллицей, status, draft, engraved,
memorial_id.
Отчёт: reports/report-TASK-0005-rev2.md (блоки А3) + образцы: pmn, obd, слитая,
карточка с цепочкой эксгумации (высота 71,3 или Окопная).
DONE + хэш коммита владельца.
