# TASK-0005 · memorial-korpech-crimea · qwen3-memorial · 50+ карточек: конвертер по D-6/D-7
Статус: NEW
Цель: расширить конвертер scripts/csv-to-markdown.mjs и прогнать текущий батч
data/csv/: ≥50 новых карточек в src/content/fallen/.
Решения (не перерешаются):
1. D-7: slug новых карточек = <транслит-ФИО>-<pmn|obd>-<document_id>.
   Ретро-переименование существующих slugs запрещено (ссылки; без затирания).
2. D-6: совпадение ФИО+дата гибели = одна карточка; второй портал доливается
   в sources[] ({org, url}); канон формулировок при слиянии — документ ОБД (П1).
3. Новое поле frontmatter conscription_location; пусто → запись в
   scripts/logs/clarify.queue.json, код причины conscription_location_empty.
4. [БезВЕСТИ]: статус «пропал без вести» → флаг missing: true + запись в
   clarify.queue.json (код missing); карточка processed; публикация — решение владельца.
5. Конвертер создаёт только новые карточки, существующие не перезаписывает.
6. Словари (units/ranks/burials/locations) байт-в-байт; дыры → clarify.queue.json,
   автоправок нет (П2, П3).
7. Фото из офицерских карточек pmn — вручную владельцем, вне задачи.
8. Батча data/csv на 50+ нет → STOP и строка в отчёте; данные не синтезировать.
Белый список: scripts/csv-to-markdown.mjs; scripts/logs/clarify.queue.json;
src/content/fallen/*.md (только новые файлы); reports/report-TASK-0005.md;
статус этого файла.
Запрещено: .gitignore, pnpm-lock.yaml, package.json; исходники зависимостей
(micromark, estree, devlop, index.js, license, readme пакетов) — читать,
копировать, выводить в Record; docs/ кроме статуса задачи; стили темы;
draft-логика donor-stub (К-06); ветки main и bak.
Контроль: pnpm exec astro check → 0 errors; новых карточек ≥50;
перезаписей существующих = 0 (в git status по fallen/ только новые файлы);
clarify.queue.json — дифф записей.
Отдача: §7.3, карточки батчами ≤20 файлов.
Отчёт: reports/report-TASK-0005.md (блоки А3) + образцы: 3 новые карточки
полным текстом (одна pmn, одна obd, одна с [БезВЕСТИ]).
Приёмка: сверка владельца по raw + коммит владельца; пересборка/выгрузка
и К-03 (динамическая локация в описаниях ленты) — отдельные задачи после закрытия.
DONE + хэш коммита владельца.