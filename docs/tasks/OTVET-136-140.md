# OTVET-136-140 — мемориал в сети; уроки в документальный формат; позывные; верификация

Координаты: `annachurasheva/memorial-korpech-crimea` · `docs/tasks/OTVET-136-140.md` · 2026-09-09
Уровень читателя: начальный. Всё — простыми словами.
Сила документов: этот > всё остальное.

## 136 · Веха: мемориал в сети
Скрин принят: https://memorial-korpech-crimea.edgeone.dev/about/ жив;
about с вашей фактурой. SC: main мемориала вылит — принято.
Отчёт деплоя лежит в репо (reports/deploy_edgeone-6-9-2026.md) —
порядок. Замечание про шаблон и китайский поисковик понял: поле baidu
в verification — НАМЕРЕННОЕ, не удалять; шаблон без «китайских»
проблемных шрифтов — осознанный выбор. Индекс решений группы «mem» —
в журнал как происхождение требований.

## 137 · Ваш черновик уроков — обработан (блок 2) и поправлен
Поправки (коротко):
- Шаг 1: авто-установка — опция, не обязанность; на кастрированной W10
  ручной путь надёжнее; в комплекте — README «что и в каком порядке».
- Шаг 2: «файлы для vscode» уточняю: заполняются файлы ПРОЕКТА
  (config, футер); .vscode — только удобство, не требование.
- Шаг 4: начинать с SC (РФ, без туннелей); EdgeOne — вторым, через туннель.
- Шаг 5: боты — только вариант «HTML meta tag» (не DNS TXT, не файл);
  значения — в config.
- Шаг 6: «отправляй по мере заполнения» = коммит→пуш→портал сам;
  журнал подскажет при ошибке.
Форма передачи персоналу (INSTRUCTION-стиль + конструкты + маркировка)
фиксируется как стандарт — пользуйтесь.

## 138 · Послания Кодерам (подать каждому в его диалог)
Кодер01 (revision-of-astro-6-1-5):
«Твой позывной в текстах проектировщика — Кодер01. Подтверди одной
строкой. Затем T-02 из docs/TASKS.md: шапка учебника „Пять шагов“.
Файлы в ответах — только с маркировкой „# ФАЙЛ: <путь>“. Отчёт —
reports/report-tutorial-header.md. Стоп после отчёта.»
Кодер02 (memorial-korpech-crimea):
«Твой позывной — Кодер02. Подтверди одной строкой. Первое задание —
ревизия твоего поля против docs/TASKS.md (T-01 v2) и живого сайта
memorial-korpech-crimea.edgeone.dev. НИЧЕГО не меняй. Отчёт —
reports/report-revision-memorial.md, формат дифф. В чат — одну строку. Стоп.»

## 139 · Верификация домена и поисковики (какие варианты брать)
Берите в каждой консоли вариант «HTML meta tag» (мета-тег), НЕ DNS TXT
и НЕ файл-загрузку: шаблон сам рисует мета-теги из config.
Нужные консоли: Yandex Webmaster (РФ), Google Search Console (зарубежь),
Baidu Webmaster (китайский контур — намеренно), Bing — по желанию.
Куда класть: src/config.ts → seo.verification → google / yandex /
baidu / bing (длинные строки content).
Там же, тем же коммитом: site.url → https://memorial-korpech-crimea.edgeone.dev
(иначе canonical/OG/sitemap смотрят на тестовый домен).
Конструкт: git add src/config.ts; commit -m "fix: verification meta, site.url свой (139)"; push.
После деплоя — кнопка «проверить» в консолях.

## 140 · «Что-то пропустила?» — чек-лист
Сделано: мемориал live; SC-зеркало main; отчёт деплоя в репо; комплекты
бумаг в обоих репо; архив OTVET; ветки bak/dev; Actions в revision жив.
Пропущено/далее:
1) site.url на свой домен + verification (139) — ближайшее движение;
2) позывные Кодер01/02 (138) — подать послания;
3) GitHub Actions включать ТОЛЬКО там, где витрина = Pages (revision,
   будущие учебные репо); мемориалу Actions не нужен — его собирает EdgeOne;
4) тег donor-v1 на astro-blog — поставим при закрытии донорской линии;
5) браузер: берите FF, если комфортнее; профиль «AI» стерильный — в силе.

