---
task_id: TASK-0200
title: Формирование начального словаря воинских званий (ranks_dict.json)
date: 2026-09-08
author: Anna Ch (Руководитель IT)
status: pending
priority: High
---

# TASK-0200: Формирование начального словаря воинских званий

## 1. Контекст
В папке `data/csv/` (включая вложенные папки) находятся исходные файлы с данными о павших. В колонке `rank` встречаются как стандартные звания, так и сокращения, а также специфические должности/звания (например, "интендант 1 ранга", "мл. политрук", "командир отделения").
Для корректной генерации карточек необходим файл-словарь `data/dictionaries/ranks_dict.json`.

## 2. Цель
Проанализировать все CSV-файлы в `data/csv/`, извлечь уникальные значения из колонки `rank` и сформировать валидный JSON-файл `data/dictionaries/ranks_dict.json` с правилами подмены.

## 3. Требования к выполнению

### 3.0. Формат отчёта (КРИТИЧЕСКИ ВАЖНО)
После выполнения операции вы **ОБЯЗАНЫ** выдать отчёт строго в этом формате:

### Выполненная операция
`[Название задачи]`

### Изменённые/Созданные файлы
- `полный/путь/к/файлу1`
- `полный/путь/к/файлу2`

### Что изменено
- Краткое описание изменения 1
- Краткое описание изменения 2

### Проверка
- [ ] JSON валиден
- [ ] Файл сохранён по указанному пути
- [ ] Количество записей: X

### Ошибки
`Нет` (или список ошибок)

### Требуется решение владельца
`Да / Нет`

### Следующая операция
`Не выполнять. Ожидать отдельный промт.`

### 3.1. Анализ данных
- Прочитать все файлы `*.csv` в директории `data/csv/` и её подпапках.
- Собрать список всех уникальных значений из колонки `rank`.

### 3.2. Формирование словаря
- **Создать файл:** `data/dictionaries/ranks_dict.json` (точный путь!)
- Для каждого уникального значения создать пару `"ключ": "значение"`.
- **Ключ** должен точно соответствовать написанию в CSV.
- **Значение** должно быть исторически корректным полным названием.

## 4. Строгие запреты
- НЕ изменять исходные CSV-файлы.
- НЕ изменять код Astro или скрипты.
- НЕ создавать другие словари (только `ranks_dict.json`).
- НЕ выполнять никаких действий, не описанных в этой задаче.

## 5. Отчёт Кодера02
*(Заполняется ИИ-агентом после выполнения)*

### Выполненная операция
`TASK-0200: Формирование ranks_dict.json`

### Изменённые/Созданные файлы
-

### Что изменено
-

### Проверка
-

### Ошибки
-

### Требуется решение владельца
`Да / Нет`

### Следующая операция
`Не выполнять. Ожидать отдельный промт.`
