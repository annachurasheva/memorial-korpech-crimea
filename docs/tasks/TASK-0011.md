# TASK-0011 · memorial-korpech-crimea · qwen3-memorial · Второй слой: свой лейаут, один язык, человеческие поля
Статус: NEW
Цель: снять с карточек наследие пост-шаблона; привести URL, языки и поля
к решениям владельца. Порядок: ДО тест-прогона конвертера (R-17), чтобы
50–70 карточек родились уже в верном лейауте.
Решения:
1. Многоязычности нет: локали en, es, ja, zh, zh-tw убрать из конфига и
   сборки; в dist не должно остаться папок en/, es/, ja/, zh/, zh-tw/, ru/.
   Канон URL второго слоя: /memorial/card/<slug>/, /memorial/, /tags/… —
   по образцу первого слоя (/posts/maj-1942-ak-monaj-enikale/).
2. Редиректы /ru/* → чистые URL одним коммитом (redirects в astro.config):
   /ru/memorial/card/x → /memorial/card/x; двери /ru/posts/ и /ru/tags/… →
   /posts/ и /tags/… (закрытие прогноза 404 из передаточного письма).
3. Карточка = чистый лист: без шапки сайта и навигации, без блока
   «Комментарии» (ADR-04); на странице: ИМЯ заголовком, блоки данных,
   источники, строка «вернуться к списку». Шапка сайта — только страницы
   первого слоя и списки.
4. Человеческие поля: первичное/текущее захоронение = формулировка документа
   (norm из словаря burials, не slug); даты по-человечески («21 марта 1942»),
   не ISO; причина выбытия = только поле «Причина» по документу (П1),
   в заголовок/description/ленты не тиражируется.
5. Дверь «Мемориал» = заглушка-сообщение (текст владельца), не 404 и не
   пустой список. Двери «Поисковики» и «Законодательство» открываются
   чистыми URL существующих постов.
6. Своя 404: dist/404.html из шаблона сайта; внутренних ссылок на голые
   slug на корне не должно быть вовсе.
Белый список: astro.config.ts; конфиг i18n; src/layouts/* (лейаут карточки);
src/pages/ru/** (демонтаж); src/components/CardFull.astro (поля); страница
404; redirects; reports/report-TASK-0011.md.
Запрещено: контент src/content/**; словари; конвертер; slug (R-15);
.gitignore; ветки main и bak.
Контроль: astro check 0 errors; в дереве dist нет en/es/ja/zh/zh-tw/ru и
giscus; на карточке нет навигации и слова «Комментарии»; строка первичного
захоронения = формулировка документа; дата гибели человеческая.
Отчёт: reports/report-TASK-0011.md (блоки А3) + скрин карточки моб 400px
до/после.
DONE + хэш коммита владельца.