# TASK-0012 · memorial-korpech-crimea · qwen3-memorial · Пять дверей: заглушка двери «Мемориал» + проверка четырёх + RSS локально
Статус: NEW
Цель: двери открыты по умолчанию; дверь «Мемориал» = заглушка-сообщение;
лента первого слоя валидна локально. Карточек на проде нет — карточный
лейаут НЕ делать (TASK-0011 отложен до возврата карточек).
Решения:
1. Первый слой не трогать (зона верстальщика, решение владельца).
   Дефекты поста maj-1942 (картинка, автор «доцент») — в К-07, не править.
2. Двери 1–4 существуют как посты/теги первого слоя: проверить в локальной
   сборке 200 и чистые URL: /posts/ (список), /tags/voinskie-soedineniya/,
   пост «Поисковики», пост «Законодательство» (слаги взять из репозитория,
   таблицу URL привести в отчёте).
3. Дверь 5: страница /memorial/ = заглушка-сообщение (текст владельца;
   если текста нет — дефолт: «Мемориал: карточки павших готовятся к
   публикации, идёт сверка документов. Пока картотек нет — обращение
   "Тем, кто держал память до нас" и лента очерков» + ссылка на
   /posts/obrashchenie/). Заглушка: без комментариев, без giscus,
   НЕ размножается по локалям, не индексирует карточки (их нет).
4. RSS первого слоя в локальной сборке: UTF-8; домен SC в ссылках;
   enclosure ≥700 px; guid стабилен; pubDate RFC822; адрес ленты со
   слэшем /rss.xml/. Лента второго слоя пуста до возврата карточек —
   это норма, не дефект.
Белый список: страница-заглушка /memorial/ (+ её лейаут-минимум);
reports/report-TASK-0012.md.
Запрещено: первый слой (посты, лейауты постов); карточки и их лейаут
(TASK-0011); i18n-конфиг целиком; словари; конвертер; .gitignore; main/bak.
Контроль: astro check 0 errors; /memorial/ = 200 с текстом заглушки;
таблица дверей: URL → 200/404; RSS-проверки из п.4 пройдены.
Отчёт: reports/report-TASK-0012.md (блоки А3) + таблица дверей + RSS-чек.
DONE + хэш коммита владельца.