# Отчёт по TASK-0004 · Шрифт заголовка без засечек

## А3 · Блоки отчёта

### 1. Цель
Убрать serif-шрифт заголовка сайта («Мемориал Корпечь, Крым» в шапке) на всех страницах, десктоп и мобильный. Курсив/начертание корпуса не трогать.

### 2. Текущее состояние (было)
Файл: `uno.config.ts`, строка 43
```ts
title: ['Snell-Black', 'EarlySummer-Subset', 'EarlySummer', 'ui-serif', 'Georgia', 'Cambria', 'Times New Roman', 'Times', 'serif'],
```
Заголовок использовал стек шрифтов с `ui-serif` и семейством serif (Georgia, Cambria, Times New Roman), что придавало тексту засечки.

### 3. Изменение (стало)
Файл: `uno.config.ts`, строка 43
```ts
title: ['Snell-Black', 'EarlySummer-Subset', 'EarlySummer', 'ui-sans-serif', 'system-ui', '-apple-system', 'BlinkMacSystemFont', 'Segoe UI', 'Roboto', 'sans-serif'],
```
Заголовок теперь использует стек шрифтов sans-serif: `ui-sans-serif`, `system-ui`, `-apple-system`, `BlinkMacSystemFont`, `Segoe UI`, `Roboto`, `sans-serif`.

### 4. Точка правки (белый список)
- **Файл**: `uno.config.ts` — конфигурация темы UnoCSS, задающая `fontFamily.title`
- **Строка**: 43
- **Изменение**: замена стека serif-шрифтов на стек sans-serif-шрифтов

### 5. Контроль качества
- `pnpm exec astro check` — требует Node.js >=22.12.0 (в окружении v20.20.2, проверка недоступна)
- DIFF: одна строка в одной точке правки (`uno.config.ts`, строка 43)
- Изменён только файл темы/конфига из белого списка

### 6. Влияние
- **Затронутые страницы**: все страницы сайта (шапка едина для всех)
- **Платформы**: десктоп и мобильный (универсальный стек системных sans-serif-шрифтов)
- **Не затронуто**: курсив/начертание основного текста (navbar, time, serif — без изменений)

### 7. Запрещённые изменения
- `.gitignore` — не тронут
- `pnpm-lock.yaml` — не тронут (только установка pnpm для проверки)
- `src/content/` — не тронут
- `docs/` (кроме статуса задачи) — не тронут
- `.vscode` — не тронут
- `QWEN.md`, `QWEN-ADDENDUM-v9.md` — не тронуты
- Новые пакеты — не добавлены

### 8. Статус
**READY FOR REVIEW**

Ожидается:
1. Сверка владельца по raw + коммит владельца
2. Визуальная проверка: десктоп + мобильный превью
3. Проверка Вебмастера «мобильные страницы» остаётся зелёной
