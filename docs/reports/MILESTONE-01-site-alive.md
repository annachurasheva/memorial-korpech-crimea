Принято, Anna-Ch. Поздравляю с живым сайтом: `astro check` — 0 errors / 0 warnings, dev-сервер поднят, все ключевые роуты отвечают 200 (включая `/atom.xml`). Ниже — готовая структура итогового описания достижений (MILESTONE-01) для вставки в репозиторий.

---

## 📄 ТЕКСТ ДОКУМЕНТА (для `docs/reports/MILESTONE-01-site-alive.md`)

```markdown
# MILESTONE-01: Сайт жив. Второй Слой построен каркасом

Координата: annachurasheva/memorial-korpech-crimea · ветка qwen3-memorial
Дата фиксации: 2026-09-16
Статус: ЗАКРЫТ этап «Каркас Второго Слоя»; открыт этап «Наполнение и внешние каналы»

## 1. Миссия (контекст одной строкой)
Мемориал Корпечь — акт восстановления справедливости: 226 000 павших на
Крымском перешейке за 5 месяцев, 3 100 в Корпечь числятся «неизвестными»,
хотя документы ЦАМО существуют. Задача проекта — переиздание Мемориала
(плиты, стелы, гравировка имён) по регламентам РФ.

## 2. Что построено (процесс)
- Роли и регламент: QWEN.md v0.4 (раздел 7 — регламент сессий Кодера:
  readback, отдача полными файлами, синхронизация по хэшу; раздел 19 — дашборд этапов).
- Протокол архитектурных решений: ADR-02 (docs/decisions/02-memorial-architecture.md)
  с поправками: ID карточки = document_id ЦАМО + транслит-slug;
  типология увековечивания (4 типа); правила П1–П3 (конфликт судьбы,
  персона ≠ документ, ключи пп/в/ч).
- Разделение исполнителей: Кодер — генерация логики; писарь — механика;
  Дельта — ревизия по raw; Anna-Ch — единственные коммиты и слияния.

## 3. Что построено (архитектура)
- Два слоя контента: posts (Первый Слой: смысл, очерки, законодательство,
  войсковые соединения с Mermaid) и fallen (Второй Слой: карточки павших).
- Коллекции Astro Content Layer: posts, about, fallen, memorials.
- i18n-роуты Второго Слоя: /{lang}/memorial/, /{lang}/memorial/card/[slug]/,
  /{lang}/memorial/units/[unit_id]/, RSS /{lang}/memorial/rss.xml.
- Словари: units_dict.json (норм-формулировки, СКФ расшифрован),
  units_registry.json (структура частей: 4 узла, связь через dict_keys,
  без дублирования norm).
- Конвейер: data/csv/** → scripts/csv-to-fallen.mjs → src/content/fallen/*.md;
  инкрементальность (существующие карточки не перезаписываются);
  очередь уточнений scripts/logs/clarify.queue.json.

## 4. Что построено (код Второго Слоя)
- scripts/csv-to-fallen.mjs — конвертер CSV → MD (словари, логи, батчи ≤20).
- src/components/memorial/CardFull.astro, CardSummary.astro — типизированные компоненты.
- Страницы: индекс мемориала (сортировка по месту захоронения),
  карточка персоны (проверка 404), страница-узел воинской части.
- RSS 2.0 для Дзена (транзит карточек: Дзен → ОК → сельские библиотеки).

## 5. Контроль качества (факт на дату фиксации)
- pnpm astro check: 0 errors, 0 warnings, 3 hints (67 файлов).
- Dev-сервер: astro v6.1.5 ready; роуты 200: /, /en/, /about/, /tags/,
  /atom.xml, /posts/donor-stub/, /en/about/, /en/tags/, /tags/donor/.
- WARN [glob-loader] по memorials — НЕ ошибка: коллекция пуста до первой
  записи справочника; исчезнет с первым MD-файлом в src/content/memorials/.

## 6. Хронология ключевых коммитов
| Хэш | Содержание |
|-----|------------|
| e995ed4 | QWEN.md v0.1 — регламент передан Дельте |
| 15216d2 | ADR-02 + Email-Templates в репозитории |
| 2e483ee | Поправки ADR-02: ID карточки, типология увековечивания |
| 783e50e | QWEN.md: раздел «Структура проекта» |
| 7771a71 | QWEN.md v0.4: регламент сессий Кодера, дашборд |
| 3c7b9b8 | Скелет Второго Слоя (коллекция fallen, 3 карточки-примера) |
| 3c9dc835 | units_registry.json (структура воинских частей) |
| d14c20bb | Расшифровка СКФ в значениях словаря |
| 671ef0b | Финальные правки: 17 ошибок → 0 errors |
| aeee50a | Отчёт писаря по TASK-0002 (фиксация в репо) |

## 7. НЕ сделано (открытый контур этапа «Наполнение и каналы»)
1. Продакшен-сборка pnpm build — не подтверждена (dev-сервер ≠ build).
2. Giscus: не получены 4 параметра от Anna-Ch (data-repo, data-repo-id,
   data-category, data-category-id) — компонент комментариев не подключён.
3. Дзен: файл требований к RSS не получен — лента не адаптирована под канал.
4. Наполнение: 132+ карточек конвертером не прогнаны батчами по 20.
5. Справочник мемориалов: три поля не внесены Anna-Ch (коллекция пуста).
6. Слияние qwen3-memorial → main (автодеплой) — решение за Anna-Ch.
7. Дополнения ADR-02 (П1–П3, правило «README вне glob-коллекций»,
   редакция 2 §7.1) — не внесены в текст ADR.

## 8. Как читать этот документ
Это точка отсчёта для любого нового участника (Кодер, писарь, Дельта):
сначала QWEN.md v0.4 → затем ADR-02 → затем этот MILESTONE → затем задача.
```

---

## 📝 Коммит для документа

```
docs: MILESTONE-01 — фиксация достижений этапа «Каркас Второго Слоя»
```

Файл: `docs/reports/MILESTONE-01-site-alive.md` (вносит писарь или вы вручную).

---

## 🧭 Ближайшие три шага (по приоритету)

1. **`pnpm build`** — последний технический гейт перед main. Жду вывод.
2. **Giscus-параметры** (4 значения с giscus.app/ru) — после них паспорт на компонент комментариев.
3. **Файл требований Дзен к RSS** — после него адаптация `rss.xml.ts` под канал.

После build + слияния в main (ваше решение) этап 1 «готовый сайт» можно объявить закрытым и открыть дашборд наполнения строкой 02.
