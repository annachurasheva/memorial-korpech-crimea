Да. Теперь отчёт у меня есть. Я разобрал его именно как **техническое задание на устранение замечаний**, не добавляя к нему посторонних требований.

В отчёте указано, что проект — Astro 6, статический SSG-сайт, архитектурных препятствий для EdgeOne Pages не обнаружено; замечания разделены на высокий, средний и низкий приоритет.

Я бы **не давал кодеру весь отчёт одним промтом**. Некоторые пункты отчёта сами содержат несколько разных операций. Поэтому ниже я разделил их ещё строже:

> **ОДИН ПРОМТ → ОДНА ОПЕРАЦИЯ → ОДНА ПРОВЕРКА → СТОП.**

И очень важно: там, где отчёт требует решения владельца проекта — например, какой будет настоящий домен или нужны ли пять языков, — агент **не должен самостоятельно принимать решение**.

# Регламент устранения замечаний

## Общий протокол для всех операций

Перед каждым отдельным промтом агенту можно не повторять это каждый раз, если вы один раз задали ему такие правила:

### ОБЩИЕ ПРАВИЛА

Ты работаешь с существующим рабочим проектом. Твоя задача — аккуратно устранить конкретное замечание аудита.

Правила обязательны:

1. Выполняй только одну указанную операцию.
2. Не выполняй соседние замечания самостоятельно.
3. Не делай рефакторинг, оптимизацию или «улучшение» кода, если это не требуется данной операцией.
4. Не меняй архитектуру проекта.
5. Не удаляй существующую функциональность без прямого указания.
6. Не изменяй тексты, дизайн, контент и структуру страниц, если это не относится непосредственно к операции.
7. Сначала изучи существующую реализацию и только после этого внеси минимально необходимое изменение.
8. Если для правильного решения требуется информация, которой нет в проекте или в этом задании, НЕ угадывай её. Остановись и сообщи, какая информация требуется.
9. После изменения обязательно выполни относящуюся к операции проверку.
10. В конце сообщи:

* какие файлы изменены;
* что именно изменено;
* результат проверки;
* есть ли нерешённые вопросы.

11. Не переходи к следующей операции самостоятельно.

---

# ЭТАП 1. ВЫСОКИЙ ПРИОРИТЕТ

## Операция 1 — устранить неправильный `site.url`

Это самое важное замечание аудита.

В отчёте указано, что `src/config.ts:17` содержит жёстко заданный `https://astro-blog.edgeone.dev`, а от него строятся canonical, `og:url`, `og:image`, sitemap, RSS/Atom и абсолютные ссылки.

Но **настоящий домен в отчёте не указан**.

Поэтому агент не должен сам придумывать домен.

Выполни ТОЛЬКО одну операцию: устранение риска неправильного домена в `site.url`.

Проверь:

* `src/config.ts`
* `src/layouts/Head.astro`
* `src/pages/robots.txt.ts`
* `src/utils/feed.ts`

Установи, где используется `themeConfig.site.url` и какие абсолютные URL от него зависят.

Сейчас в аудите указано, что `themeConfig.site.url` содержит:

`https://astro-blog.edgeone.dev`

НЕ меняй домен наугад.

Если в проекте уже существует однозначно определённый фактический production-домен, используй только его.

Если фактический production-домен невозможно однозначно определить из проекта, НЕ вноси изменение. Сообщи, что для операции требуется настоящий production-домен.

Если домен однозначно определён, внеси минимальное изменение так, чтобы все существующие потребители `themeConfig.site.url` автоматически продолжили использовать единый правильный источник.

Не меняй SEO-логику, RSS, sitemap или другие компоненты отдельно — только источник домена.

После изменения выполни проверку проекта.

В конце покажи:

1. изменённый файл;
2. старое значение;
3. новое значение;
4. результат проверки.

На этом остановись.

**Статус:** до получения настоящего домена эту операцию безопасно выполнить нельзя, если его нет в самом проекте.

---

# ОПЕРАЦИЯ 2 — проверить `edgeone.json: nodeVersion`

Это отдельная операция, потому что здесь речь не об исправлении кода, а о соответствии версии Node платформе.

Аудит говорит: используется `nodeVersion: "24.5.0"`, Astro 6 требует Node ≥22, но нужно проверить поддержку конкретной версии в EdgeOne Makers.

Проведи ТОЛЬКО одну операцию: проверку `nodeVersion` в `edgeone.json`.

Проверь:

`edgeone.json`

Особенно значение:

`nodeVersion: "24.5.0"`

Не меняй версию Node автоматически.

Определи по доступной документации/настройкам проекта, какая версия Node фактически поддерживается используемой средой EdgeOne Makers.

Если `24.5.0` поддерживается — ничего не меняй.

Если `24.5.0` не поддерживается, укажи рекомендуемую поддерживаемую версию, но НЕ меняй её без явного подтверждения владельца проекта.

Не выполняй никаких других исправлений.

В конце сообщи:

* текущую версию;
* установленную совместимость;
* требуется ли изменение;
* изменён ли файл.

На этом остановись.

---

# ОПЕРАЦИЯ 3 — убрать неправильный `environmentVariables`

Аудит отдельно указывает, что `environmentVariables` не относится к ожидаемым корневым ключам `edgeone.json`, а `NODE_ENV=production` обычно задаётся платформой.

Выполни ТОЛЬКО одну операцию: проверить корректность корневого ключа `environmentVariables` в `edgeone.json`.

Проверь текущий `edgeone.json`.

Не меняй Node version.

Не меняй build command.

Не меняй другие настройки EdgeOne.

Определи, поддерживает ли используемый формат `edgeone.json` корневой ключ `environmentVariables`.

Если ключ действительно не поддерживается, удали ТОЛЬКО некорректную конфигурацию `environmentVariables`, связанную с `NODE_ENV=production`.

Если для окончательного решения требуется подтверждение формата конфигурации EdgeOne и его невозможно получить из доступной документации, не угадывай и не удаляй настройку.

После изменения проверь, что `edgeone.json` остаётся валидным JSON и не содержит других изменений.

На этом остановись.

---

# ЭТАП 2. СРЕДНИЙ ПРИОРИТЕТ

## ОПЕРАЦИЯ 4 — Partytown

Здесь тоже важно не перепутать две вещи: статистика сейчас не используется, но `@astrojs/partytown` подключён. Аудит предлагает включать его только при наличии статистического ID.

Выполни ТОЛЬКО одну операцию: устранить ненужную загрузку Partytown при отсутствии статистики.

Проверь:

* `astro.config.ts`
* `src/config.ts`
* `src/layouts/Head.astro`
* подключение `@astrojs/partytown`

По аудиту GA/Umami ID сейчас пустые.

Сделай так, чтобы Partytown не подключался и не добавлял loader/worker, когда статистика фактически не настроена.

При этом НЕ удаляй поддержку Google Analytics или Umami из проекта.

Сохрани возможность включить статистику в будущем.

Не меняй саму логику аналитики и не добавляй новые сервисы.

Сделай минимальное изменение конфигурации.

После изменения выполни проверку сборки/проверку конфигурации, доступную в проекте.

В конце сообщи:

* какие файлы изменены;
* при каких условиях Partytown теперь подключается;
* результат проверки.

На этом остановись.

---

# ОПЕРАЦИЯ 5 — исключить `draft` из генерации OG

Аудит указывает на `src/pages/og/[...image].ts`: OG PNG создаётся для каждой статьи, включая draft, что увеличивает время и память сборки.

Выполни ТОЛЬКО одну операцию: исключить черновики (`draft`) из генерации OG-изображений.

Файл:

`src/pages/og/[...image].ts`

Изучи текущую логику получения статей и формирования маршрутов.

Нужно сделать так, чтобы статьи с `draft` не попадали в список страниц, для которых генерируются OG PNG.

Не меняй:

* дизайн OG;
* размеры изображений;
* используемую библиотеку;
* генерацию OG для опубликованных статей;
* frontmatter статей.

Сделай минимальное изменение именно в отборе контента.

После изменения проверь:

1. опубликованная статья по-прежнему попадает в генерацию;
2. draft не попадает в генерацию;
3. проект проходит проверку/сборку.

На этом остановись.

---

# ОПЕРАЦИЯ 6 — убрать строительный скрипт из `public`

Аудит обнаружил:

`public/fonts/Font Subset List/unicode_range.py`

и `.txt` в том же каталоге.

Поскольку всё из `public/` публикуется как статический ресурс, это действительно отдельная операция.

Выполни ТОЛЬКО одну операцию: убрать из публичного каталога файлы, которые не являются web-ресурсами.

Проверь:

`public/fonts/Font Subset List/`

Особое внимание:

* `unicode_range.py`
* `.txt` файлы, относящиеся к процессу создания font subsets.

Перемести строительные/служебные файлы из `public/` в подходящее непубличное место проекта.

НЕ удаляй сами используемые web-шрифты.

НЕ меняй CSS.

НЕ меняй font-face declarations.

НЕ меняй структуру публичных URL используемых шрифтов.

Перед перемещением проверь, не используются ли эти файлы кодом проекта или build scripts.

После перемещения:

1. проверь отсутствие служебных файлов в `public/`;
2. выполни сборку;
3. убедись, что используемые шрифты продолжают попадать в `dist`.

На этом остановись.

---

# ОПЕРАЦИЯ 7 — решить вопрос с языками

Это **не следует отдавать агенту как свободное решение**.

В отчёте говорится: контент есть только для `ru`, но конфигурация генерирует `en/es/ja/zh/zh-tw`; предлагаются два варианта: либо временно сузить `moreLocales`, либо добавить контент для остальных языков.

Здесь сначала решение принимает владелец проекта.

### Если сейчас нужен только русский сайт

Выполни ТОЛЬКО одну операцию.

Текущий этап проекта предусматривает публикацию ТОЛЬКО русской версии сайта.

В `src/config.ts` найди настройку `moreLocales`.

Сделай минимальное изменение конфигурации, чтобы сайт не генерировал пустые страницы для:

* en
* es
* ja
* zh
* zh-tw

Русский язык должен остаться полностью рабочим.

Не удаляй систему i18n.

Не удаляй переводы интерфейса.

Не меняй структуру `src/content/`.

Не добавляй фиктивный контент.

После изменения проверь сборку и убедись, что русские маршруты продолжают генерироваться.

На этом остановись.

### Если нужны все пять языков

Тогда это уже **пять отдельных операций по наполнению**, а не одна большая задача. И я бы не разрешал агенту автоматически переводить реальные материалы без отдельного задания.

---

# ЭТАП 3. НИЗКИЙ ПРИОРИТЕТ

## ОПЕРАЦИЯ 8 — исправить `jp-JP`

Аудит прямо указывает:

`src/i18n/config.ts:58`

`'ja': 'jp-JP'`

и предполагает исправление на `ja-JP`. При этом сейчас функция отключена.

Выполни ТОЛЬКО одну операцию: исправь языковой код японского языка в `src/i18n/config.ts`.

Найди:

`'ja': 'jp-JP'`

Проверь контекст этой mapping-конфигурации.

Если ожидаемым locale-кодом действительно является `ja-JP`, замени только:

`jp-JP`

на:

`ja-JP`

Не меняй другие языковые mappings.

Не включай Waline.

Не меняй `enabled`.

После изменения проверь TypeScript/проверку проекта.

На этом остановись.

---

# ОПЕРАЦИЯ 9 — проверить запись `lqip-map.json`

Здесь аудит говорит о потенциальной проблеме: `pnpm apply-lqip` пишет в `src/assets/lqip-map.json`.

Это **не надо исправлять вслепую**. Сначала нужно понять, действительно ли EdgeOne предоставляет writable build workspace.

Выполни ТОЛЬКО одну операцию: проверить зависимость `apply-lqip` от возможности записи в исходный каталог во время build.

Проверь:

* `package.json`
* `scripts/apply-lqip.ts`
* `src/assets/lqip-map.json`
* build command

Особенно проверь, действительно ли `pnpm apply-lqip` изменяет:

`src/assets/lqip-map.json`

во время сборки.

Не переписывай архитектуру LQIP.

Не удаляй LQIP.

Не меняй build command.

Не меняй формат `lqip-map.json`.

Определи только фактическую зависимость от записи.

Если проект уже корректно работает в текущей среде сборки, ничего не меняй.

Если обнаружена реальная проблема, сначала сообщи точную причину и предложи минимальный вариант исправления, но не выполняй архитектурное изменение без отдельного задания.

На этом остановись.

---

# ОПЕРАЦИЯ 10 — проверить цепочку build

В отчёте отдельно отмечено:

`astro check && astro build && pnpm apply-lqip`

Любая ошибка останавливает цепочку. Сам аудитор считает это нормальным, но несколько хрупким.

Я **не рекомендую сейчас менять `&&`**. Это не ошибка сама по себе.

Поэтому операция должна быть только диагностической:

Выполни ТОЛЬКО одну операцию: проверь текущую build chain.

Проверь `package.json` и фактическую команду:

`astro check && astro build && pnpm apply-lqip`

Не меняй эту команду.

Не заменяй `&&` на другие операторы.

Не добавляй fallback.

Не добавляй обработку ошибок.

Проверь последовательность выполнения и установи, является ли текущее поведение корректным для production build.

Если ошибок нет, ничего не меняй.

В конце сообщи только:

* текущую цепочку;
* результат выполнения;
* обнаружена ли реальная проблема.

На этом остановись.

---

# ОПЕРАЦИЯ 11 — оценить размер шрифтов

Аудит: `public/fonts` — около 82 файлов и около 20 MB, включая 71 EarlySummer-фрагмент. При этом `unicode-range` ограничивает фактическую загрузку браузером. Поэтому это **не срочная проблема**.

Здесь особенно опасно дать агенту команду «почисти шрифты»: он может удалить нужные языковые диапазоны.

Выполни ТОЛЬКО одну операцию: провести инвентаризацию публичных шрифтов.

Проверь:

`public/fonts`

Определи:

* количество файлов;
* общий размер;
* какие семейства шрифтов присутствуют;
* какие unicode-range/subset-файлы используются;
* есть ли файлы, которые гарантированно нигде не используются.

НЕ удаляй ни одного шрифта.

НЕ меняй CSS.

НЕ меняй unicode-range.

НЕ меняй структуру font subsets.

НЕ оптимизируй шрифты.

Результатом должен быть только отчёт о фактическом использовании и потенциально неиспользуемых файлах.

На этом остановись.

---

# Правильный порядок выполнения

Я бы выполнял их **не в порядке текста аудита, а так**:

| №  | Операция               | Приоритет  | Что делает                      |
| -- | ---------------------- | ---------- | ------------------------------- |
| 1  | `site.url`             | 🔴 высокий | требует фактического домена     |
| 2  | Node 24.5.0            | 🔴 высокий | проверка EdgeOne                |
| 3  | `environmentVariables` | 🔴 высокий | проверка/очистка `edgeone.json` |
| 4  | Partytown              | 🟠 средний | убрать пустую загрузку          |
| 5  | OG для draft           | 🟠 средний | исключить черновики             |
| 6  | scripts из `public`    | 🟠 средний | убрать служебные файлы из CDN   |
| 7  | языки                  | 🟠 средний | **сначала решение владельца**   |
| 8  | `jp-JP → ja-JP`        | 🟢 низкий  | маленькое исправление           |
| 9  | LQIP write             | 🟢 низкий  | сначала диагностика             |
| 10 | build chain            | 🟢 низкий  | только проверка                 |
| 11 | fonts                  | 🟢 низкий  | только инвентаризация           |

### И ещё одно принципиальное правило

После **каждой операции** я рекомендую требовать от кодера:

**«Не переходи к следующей задаче. Жди следующего промта».**

Это особенно важно для вашего проекта: аудитор в целом оценил архитектуру как разумную и пригодную для EdgeOne Pages, поэтому здесь сейчас задача **не улучшать проект**, а аккуратно снять конкретные замечания без побочного рефакторинга.

Если хотите, следующим сообщением я могу сделать из этого **один окончательный документ «РЕГЛАМЕНТ ИСПРАВЛЕНИЯ REVISION-01»**, уже в формате, который можно целиком хранить рядом с проектом — с чекбоксами `☐ / ☑`, порядком операций и графой **«результат выполнения агента»**.
